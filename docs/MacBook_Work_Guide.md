# 💻 MacBook Pro リモートワーク設定ガイド

外出先（MacBook Pro）で `AI_TOOL_NEWS` の開発・運用を行うための手順書です。
Windows環境とシームレスに連携するために、**「Gitによる同期」** と **「秘密鍵の手動移行」** の2点が重要になります。

---

## 🚀 1. 初回セットアップ（一度だけ実行）

MacBook Proで初めて作業を開始する際の手順です。

### 1-1. リポジトリのクローン
ターミナルを開き、作業用ディレクトリで以下のコマンドを実行します。

```bash
# 作業用ディレクトリへ移動（例）
cd ~/Desktop

# GitHubからコードを取得
git clone https://github.com/TadFuji/AI_TOOL_NEWS.git
cd AI_TOOL_NEWS
```

### 1-2. 秘密鍵（.env）の移行【重要】
`.env` ファイルにはAPIキーが含まれているため、GitHubにはアップロードされていません。
以下の手順でWindowsからMacへ安全に移行してください。

1. **Windows側**: `AI_TOOL_NEWS` フォルダ内の `.env` ファイルの内容をコピーします。
2. **Mac側**: VS Codeなどで `AI_TOOL_NEWS` フォルダを開き、直下に `.env` という名前の新しいファイルを作成します。
3. **貼り付け**: コピーした内容を貼り付けて保存します。

> **Note**: その他、Google Cloudの認証ファイル（JSON）がある場合も同様に手動でコピーが必要です。

### 1-3. 環境構築
Pythonの仮想環境を作成し、必要なライブラリをインストールします。

```bash
# 仮想環境の作成
python3 -m venv .venv

# 仮想環境の有効化
source .venv/bin/activate

# 同期の確認（pipのアップグレードは任意）
pip install --upgrade pip

# ライブラリのインストール
pip install -r requirements.txt
```

---

## 🔄 2. 日々の作業フロー（同期手順）

データの先祖返り（競合）を防ぐため、場所を移動する際は**「Pushしてから移動、Pullしてから開始」**を徹底してください。

### 🏠 Windows を離れるとき
作業が終わったら、必ずGitHubへ最新状態を送信します。

```powershell
git add .
git commit -m "Windowsでの作業終了: [作業内容]"
git push
```

### 🍎 Mac で作業を始めるとき
カフェなどに着いたら、まず最新の状態を受信します。

```bash
cd ~/Desktop/AI_TOOL_NEWS
git pull
# 仮想環境に入っていなければ入る
source .venv/bin/activate
```

### 🍎 Mac を離れるとき
作業が終わったら、同様に送信します。

```bash
git add .
git commit -m "Macでの作業終了: [作業内容]"
git push
```

### 🏠 Windows に戻ったとき
自宅に着いたら、作業を再開する前に受信します。

```powershell
git pull
```

---

## ⚠️ トラブルシューティング

**Q. Pushし忘れて外出してしまった！**
A. 可能であればWindowsマシンをリモート操作してPushしてください。それができない場合、Macで作業を進めても構いませんが、帰宅後に「コンフリクト（衝突）」が発生する可能性があります。その際は `git merge` 等で解消作業が必要になります。

**Q. ライブラリを追加した（`pip install xxx`した）**
A. `requirements.txt` も更新してPushしてください。
```bash
pip freeze > requirements.txt
```
移動先のPCで `pip install -r requirements.txt` を再実行すれば、同じ環境になります。
